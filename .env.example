# Copy to .env for local-only provider integration tests.
# Never commit real API keys.

GLM_API_KEY=
GLM_BASE_URL=https://api.z.ai/api/paas/v4
GLM_MODEL=glm-5.2
GLM_TIMEOUT_SECONDS=30
GLM_MAX_RETRIES=2
GLM_RETRY_BACKOFF_SECONDS=0.25
